{
  "compilerOptions": {
    "target": "esnext",  // Компиляция в последний доступный стандарт ECMAScript
    "module": "esnext",  // Использование ES-модулей
    "moduleResolution": "node",  // Разрешение модулей как в Node.js
    "jsx": "react-jsx",  // Использование автоматического JSX-режима для React 17+
    "esModuleInterop": true,  // Для импорта CommonJS модулей в стиле ES6
    "strict": true,  // Включение строгой проверки типов
    "skipLibCheck": true,  // Пропуск проверки типов в библиотеках
    "forceConsistentCasingInFileNames": true,  // Обеспечение согласованности регистров в путях
    "outDir": "./dist",  // Папка для выходных файлов
    "allowJs": true,  // Разрешение работы с JavaScript-файлами
    "checkJs": false,  // Не проверять файлы .js на ошибки TypeScript
    "resolveJsonModule": true,  // Разрешение работы с JSON модулями
    "noEmit": false,  // Не отключать вывод компилятора (оставим для использования с Vite)
    "skipDefaultLibCheck": true  // Пропуск проверки стандартных библиотек
  },
  "include": [
    "src/**/*"  // Указание, что все файлы внутри папки src должны быть обработаны
  ],
  "exclude": [
    "node_modules",  // Исключаем node_modules
    "dist"  // Исключаем папку dist
  ]
}
